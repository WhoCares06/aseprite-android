<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/nav_home">

    <fragment
        android:id="@+id/nav_home"
        android:name="com.aseprite.android.ui.HomeFragment"
        android:label="@string/app_name"
        tools:layout="@layout/fragment_home" />

    <fragment
        android:id="@+id/nav_editor"
        android:name="com.aseprite.android.ui.EditorFragment"
        android:label="@string/editor"
        tools:layout="@layout/fragment_editor" />

    <fragment
        android:id="@+id/nav_recent"
        android:name="com.aseprite.android.ui.RecentFragment"
        android:label="@string/recent_files"
        tools:layout="@layout/fragment_recent" />

    <fragment
        android:id="@+id/nav_settings"
        android:name="com.aseprite.android.ui.SettingsFragment"
        android:label="@string/settings"
        tools:layout="@layout/fragment_settings" />

</navigation>